node {
	environment {
        DISABLE_AUTH = 'true'
        DB_ENGINE    = 'sqlite'
    }     
    stage "Build"
	
	// make the output dir
	echo "Database engine is ${DB_ENGINE}"
	
	// print
	echo "DISABLE_AUTH is ${DISABLE_AUTH}"

	
	stage "Finish"
	
	echo "done'

}
